<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pasada_driver"
        tools:targetApi="31">
        <activity
            android:name=".HomePageOffline"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoadingMain"/>
        <activity android:name=".HomePageOnline"/>
        <activity android:name=".WelcomePage"/>
        <activity android:name=".LoginPage"/>
        <activity android:name=".OfflineBookingsActivity"/>
        <activity android:name=".OnlineBookingsActivity"/>
        <activity android:name=".PadalaDetails"/>
        <activity android:name=".HatidSundoDetails"/>



    </application>

</manifest>